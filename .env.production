# Production Environment Configuration

# Database Configuration - Using Prisma Accelerate (recommended for serverless)
DATABASE_URL="prisma+postgres://accelerate.prisma-data.net/?api_key=YOUR_API_KEY_HERE"

# Application Configuration
NODE_ENV=production

# Security (use a strong secret)
JWT_SECRET="REPLACE_WITH_STRONG_SECRET_IN_PRODUCTION"
