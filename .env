# Build-time only environment variables for Vercel
# This file is used during the build process on Vercel
# For runtime, set environment variables in the Vercel dashboard

# Placeholder DATABASE_URL (not used at runtime)
DATABASE_URL="postgresql://placeholder:placeholder@localhost:5432/placeholder"

# Build environment
NODE_ENV=production 

# Indicate this is a build environment
IS_VERCEL_BUILD=true
