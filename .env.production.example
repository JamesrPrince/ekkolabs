# Production Environment Configuration
# Replace with actual values and rename to .env.production

# Database Configuration with Prisma Accelerate (recommended for Vercel deployment)
DATABASE_URL="prisma+postgres://accelerate.prisma-data.net/?api_key=YOUR_API_KEY_HERE"

# Or direct connection to PostgreSQL database (without Accelerate)
# DATABASE_URL="postgresql://username:password@hostname:port/database"

# Application Configuration
NODE_ENV=production
PORT=5001

# Security (use a strong secret)
JWT_SECRET="replace-with-secure-secret-in-production"
